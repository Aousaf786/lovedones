CREATE TABLE users (
    id BigInt(20) NOT NULL AUTO_INCREMENT,
    name varchar(100) NOT NULL,
    email varchar(255) NOT NULL,
    password varchar(150) NOT NULL,
    fcm_token TEXT,
    api_token varchar(150),
    phone_number varchar(50),
    address varchar(255),
    role ENUM('Admin','User','Employee') DEFAULT 'User',
    status tinyint(1) DEFAULT '1',
    profile_img varchar(255),
    additional_info TEXT,
    subscription_status tinyint(1) DEFAULT '0',
    family_subscription_status tinyint(1) DEFAULT '0',
    timezone varchar(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (ID)
);

INSERT INTO users (name, email, password, api_token, role) VALUES ("Admin", "admin@domain.com", "$2b$12$iH5vm4Lmcn6LVAthiX8a/ewc.Z4mYYAgM22eon2v5pR63jpf/vR1a", "SkHJdlYGORSBrVg48l5ZJ5223dfa", "Admin");

ALTER TABLE `users` ADD `subscription_package` VARCHAR(255) NULL DEFAULT NULL AFTER `family_subscription_status`;